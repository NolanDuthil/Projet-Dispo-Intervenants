export type User = {
    id: string;
    nom: string;
    prenom: string;
    email: string;
    password: string;
  };

  export type Intervenants = {
    id: string;
    nom: string;
    prenom: string;
    email: string;
  };